export default class Book {
  constructor(id, author, title, category, priority) {
    this.id = id;
    this.author = author;
    this.title = title;
    this.category = category;
    this.priority = priority;
  }
}
